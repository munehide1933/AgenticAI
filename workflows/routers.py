"""
Public portfolio edition.

Routing strategy internals are intentionally omitted.
See docs/public/IMPLEMENTATION_PATTERNS_EN_JA.md for the decision pattern summary.
"""


def route_after_understanding(state):
    raise NotImplementedError(
        "Routing internals are private in the public portfolio edition."
    )


def route_after_search(state):
    raise NotImplementedError(
        "Routing internals are private in the public portfolio edition."
    )


def route_after_initial_analysis(state):
    raise NotImplementedError(
        "Routing internals are private in the public portfolio edition."
    )


def route_after_reflection(state):
    raise NotImplementedError(
        "Routing internals are private in the public portfolio edition."
    )


def route_after_detailed_analysis(state):
    raise NotImplementedError(
        "Routing internals are private in the public portfolio edition."
    )

